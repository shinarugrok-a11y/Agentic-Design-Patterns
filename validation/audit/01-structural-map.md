# 1. Structural map

Recorded before reorganization. The only tree change this audit made is the addition of `validation/audit/` plus corrections in `README.md`, `chapter_notebooks/README.md`, and `tools/validate.py`. Canonical PDF, ground-truth text, notebooks, and skill cards were not moved.

## Root

| Path | Purpose | Referenced | Executable | Docs only | Generated | Canonical | Duplicated | Stale at audit start | External dependency |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `Agentic_Design_Patterns_Complete.pdf` | Book PDF, 18,537,352 bytes, 458 pages (`pdfinfo`) | `ground-truth/README.md`, root README | no | source | no (producer PyPDF2) | yes, book bytes | no | README said 424 pages and `book/` path | none to open |
| `README.md` | Public contract | GitHub landing page | no | yes | no | yes, after this audit | no | yes, many operational claims | badges and links |
| `AGENTS.md` | Agent navigation for the skill library | `tools/validate.py`, root README | no | yes | no | yes | no | validator expected the old phrase "Do not read the PDF" | none |
| `manifest.json` | 21 skill records | `AGENTS.md`, `tools/validate.py` | data | no | token estimates can be rewritten by `--sync` | yes | fields also live in each `SKILL.md` frontmatter | no, validator frontmatter check passed | none |

## `ground-truth/`

| Path | Purpose | Notes |
| --- | --- | --- |
| `agentic_design_patterns.txt` | Layout-preserving text of the PDF | 882,642 bytes, 17,659 lines, 458 non-empty form-feed pages. Canonical readable source for audits. |
| `README.md` | States the extract command and baseline commit `fede537` | PDF blob itself was added in `7ddebbc` and not modified afterward (`git log -- Agentic_Design_Patterns_Complete.pdf`). |

Classification: **canonical source extract**, with a small layout delta versus Poppler 24.02.0 (see evidence map).

## `skills/` (21 directories)

Each id in `manifest.json` has `SKILL.md`, `references/patterns.md`, `references/deep-dive.md`, `examples/minimal.py`.

| Role | What it is |
| --- | --- |
| `SKILL.md` | Derived pattern card. Body token counts 188–234 (`cl100k_base`). |
| `references/patterns.md` | Short derived notes, 209–282 tokens. |
| `references/deep-dive.md` | Longer derived notes, including framework-shaped code. Not executed as a suite. |
| `examples/minimal.py` | Offline stub. **Executable.** All 21 exited 0 under `tools/validate.py`. Standard library only. |

Classification: **derived, active**. Canonical for agent loading. Not a second copy of the book.

## `models/`

`fable-5-1.md` (40 lines), `grok-4-6.md` (40 lines), `muse.md` (39 lines). Documentation-only loading guidance. Context-window figures are unverified vendor-style claims. No code imports these files.

## `chapter_notebooks/`

| Kind | Count | Classification |
| --- | --- | --- |
| `Chapter_*.ipynb` | 56 | Reference snippets. Counts per chapter: 2,3,2,3,5,2,6,5,1,5,1,1,1,3,3,2,4,3,2,1,1 |
| `Chapter_*.SKILL.md` | 56 | Generated duplicates of `skills/<id>/SKILL.md` |
| Appendix placeholders | 7 | Historical. Google Drive instructions. Not the appendix text (that text is in the PDF) |
| `Appendix_C_(Code).ipynb` | 1 | Illustrative cells; stored `NameError` outputs |
| `Appendix_Pydantic.ipynb` | 1 | Pydantic example; `pydantic` not installed in the audit environment |
| `README.md` | 1 | Index. Filename list matched the tree. "58 complete runnable notebooks" did not |

Machine-readable rows: [notebook-inventory.json](notebook-inventory.json).

## `tools/`

`validate.py` only. Executable. Depends on `tiktoken` (not vendored) and on `git` to compare notebooks and the PDF with `origin/main`. It runs every `examples/minimal.py`.

## `validation/audit/`

Added by this audit. Documentary evidence. Not generated by the book pipeline.

## Not present

No `tests/` directory, no `requirements.txt`, no `LICENSE`, no `CONTRIBUTING.md`, no `book/` directory, no `.env`, no application `src/`, no Docker, no CI workflow, no database, no frontend.

## Historical / deprecated material

- Seven appendix placeholder notebooks and the Google Drive / Google Docs URLs inside them and in the old README.
- Root README clone URL, issue links, and license badge aimed at `evoiz/agentic-design-patterns` while this remote is `shinarugrok-a11y/Agentic-Design-Patterns`.
- `evoiz/Agentic-Design-Patterns` still exists. Its `main` SHA is `e11e6fb`, which is an ancestor of this repository. It does not contain the skill library.

## Executable surface (complete)

| Entry | What running it does |
| --- | --- |
| `python3 tools/validate.py` | Structural checks, token simulation, 21 offline examples |
| `python3 skills/<id>/examples/minimal.py` | One offline stub |
| `python3 tools/validate.py --sync` | Rewrites token estimates and `.SKILL.md` companions |
| Notebook cells | Only when a human installs extra libraries; not a supported batch entry point |
